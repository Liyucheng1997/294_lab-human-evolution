{
  "name": "human-evolution-3d",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build && esbuild worker/index.ts --bundle --format=esm --platform=browser --target=es2022 --outfile=dist/server/index.js",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "^0.1.0",
    "@types/three": "^0.180.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.0",
    "vite": "^8.0.13"
  }
}
